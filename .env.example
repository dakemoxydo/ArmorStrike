# Supabase — облачный бэкенд ArmorStrike (см. src/lib/supabaseClient.ts).
# Скопируйте в .env и подставьте значения из Dashboard → Project Settings → API.
# .env не коммитится (.gitignore); секреты только здесь, никогда в коде.
# Источник истины для URL/ключей — .env; URL ниже дублирует встроенный fallback
# в supabaseClient.ts, код при отсутствии .env только варнит (без смены поведения).
VITE_SUPABASE_URL=https://tukylkqpvzltzqrnfutc.supabase.co
VITE_SUPABASE_ANON_KEY=вставьте-anon-public-ключ
VITE_SUPABASE_PUBLISHABLE_KEY=вставьте-publishable-ключ-если-выдан-вместо-anon

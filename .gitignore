# Dependencies & build
node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Local dev evidence (screenshots via scripts/screenshot.sh; numbers live in PROGRESS.md)
screenshots/

# Env / secrets
.env
.env.*
!.env.example

# Graphify CLI regenerates artifacts on git hooks if still installed locally
graphify-out/

# Agent / IDE local configs (machine-specific paths)
.agents/
.workbuddy-ai/
.kilocode/
.qwen/
.claude/
.cursor/
.vscode/
*.local.json

# OpenCode — local tooling only (no shared project config)
.opencode/

# Obsidian — keep shared vault settings; ignore personal UI state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json
.obsidian/appearance.json
.obsidian/plugins/
.obsidian/themes/
.obsidian/snippets/
